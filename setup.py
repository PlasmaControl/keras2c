# setup.py

from setuptools import setup, find_packages

setup(
    name='keras2c',
    version='0.1',
    packages=['keras2c'],
    package_dir={'':'src'},
    install_requires=[
        # Add dependencies here
    ],
)